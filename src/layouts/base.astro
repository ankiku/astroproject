---
import Head from "@/components/bases/head.astro";
import Header from "@/components/shared/header.astro";
import Footer from "@/components/shared/footer.astro";
import type { Entry } from "@/lib/types";
import { getMeta } from "@/lib/utils/getMeta";

const { entry } = Astro.props;

/* ✅ SAFE meta handling */
let meta;

if (entry?.collection) {
  meta = getMeta(entry);
} else {
  meta = {
    title: entry?.data?.title || "News",
    description: entry?.data?.description || "",
    image: entry?.data?.cover || "",
  };
}

/* ✅ SAFE html attributes (multi-domain compatible) */
const locale = "en"; // or derive from entry
const dir = "ltr"; // safe default
---

<html lang={locale} dir={dir}>
  <Head meta={meta} />
  <body class="flex flex-col">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
